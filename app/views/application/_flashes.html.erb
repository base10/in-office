<% if flash.any? %>
  <div class="flashes">
    <% user_facing_flashes.each do |key, value| -%>
      <div class="flash-<%= key %>" data-role="feedback"><%= value %></div>
    <% end -%>
  </div>
<% end %>
